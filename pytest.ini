[pytest]
asyncio_mode = auto
markers =
    unit: pure logic tests, no external infra required
    integration: needs Postgres + Redis (docker-compose up)
    e2e: full pipeline test, needs Postgres + Redis + migrated/seeded DB
